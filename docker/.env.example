# 复制本文件为 `docker/.env` 后按需修改（不要将 `.env` 提交到 Git）。

# 与 stockManager / carSales / Astock 等同机部署时必设，避免 Compose 项目名冲突
COMPOSE_PROJECT_NAME=adastra

# 默认 8083，避开 stockManager 8080、carSales 8081、Astock 8082
FRONTEND_PUBLISH_PORT=8083
